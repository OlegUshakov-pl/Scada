Powershell

cd D:\ai_projects\Scada


podman compose -f composer.yml up --build -d

---------------
podman-compose up -d

podman-compose -f composer.yml up -d

podman-compose -f composer.yml down
